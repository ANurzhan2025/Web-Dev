#import re

#n = int(input())

#for _ in range(n):
    #line = input()
    #line = re.sub(r'(?<= )&&(?= )', lambda x: 'and', line)
    #line = re.sub(r'(?<= )\|\|(?= )', lambda x: 'or', line)
    
    #print(line)

